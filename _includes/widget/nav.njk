    <header class="p-3">
      <p class="text-center"><a href="{{ '/' | url }}">{{ metadata.title }}</a></p>
      <nav class="navbar grape p-3 rounded">
      {%- for entry in collections.all | eleventyNavigation %}
       <a href="{{ entry.url | url }}" class="nav-link text-white">{{ entry.title }}</a>
      {%- endfor %}      
       <a href="/order/#!/Resto-Cafe/c/55139027" class="nav-link text-white">Order</a>
      </nav>
    </header>